/*
    JavaScript engine ignores whitespace. 
        However, you can use whitespace to format the code to make it easy to read and maintain.

    The following JavaScript code doesn’t use whitespace:

*/

let formatted = true; if (formatted) {console.log('The code is difficult to read');}



//It is equivalent to the following code that uses whitespace. Hence, this code is much easier to read:

let formatted1 = true;

if (formatted) {
  console.log('The code is easy to read');
}